
identification division.
	program-id. signed.
	author. dfdfd df ddf df .
	date-written. dfdffd fd df.
environment division.
	configuration section.
	source-computer. ddfffffffffffff.
	object-computer. LLfffffffffffffffffffVM.
data division.
	working-storage section.
		77 a pic s9(18) value 42.
		77 A pic s9(18) value 42.
		77 b pic s9(18).
procedure division.
	main section.
	start.
		perform loop until (1 = 0) or (not 1) or 0 or (1 - 1) or (not 2013).
		stop run.
	loop.
		display a.
		display ''.
		display A.
		display ''.
		accept b.
		add b to a.
		subtract -b from A.

end program signed.